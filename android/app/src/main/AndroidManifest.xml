<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 13+ asks before anything can post; the sweep alarm needs it. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Notifee's library declares the exact-alarm permission for apps that
         need one. The sweep is scheduled inexactly on purpose, so drop it
         rather than ask for a permission Play reviews and we never use. -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:node="remove" />

    <!-- Android 11+ package visibility: without these the speech-recognition
         and camera intents resolve to nothing and the capture silently fails. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- What the widgets open the app with; parsed by src/widgets/links.ts.
             The activity is singleTask, so a tap while the app is already in
             memory arrives at onNewIntent rather than starting a second copy. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="stilldo" />
        </intent-filter>
      </activity>

      <!-- The home-screen widgets. Android has no lock-screen widgets to place
           these on: keyguard widgets were removed in Android 5, and what came
           back in Android 15 is tablet-only. The lock screen half of this
           feature is iOS. -->
      <receiver
          android:name=".widgets.QuickCaptureWidget"
          android:exported="false">
          <intent-filter>
              <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
          </intent-filter>
          <meta-data
              android:name="android.appwidget.provider"
              android:resource="@xml/widget_quick_capture_info" />
      </receiver>

      <receiver
          android:name=".widgets.RightNowWidget"
          android:exported="false">
          <intent-filter>
              <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
          </intent-filter>
          <meta-data
              android:name="android.appwidget.provider"
              android:resource="@xml/widget_right_now_info" />
      </receiver>
    </application>
</manifest>
